import React from "react";
import ReactDOM from "react-dom/client";
import ClarityOS from "./ClarityOS.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClarityOS />
  </React.StrictMode>
);